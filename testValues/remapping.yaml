apiVersion: helm.cattle.io/v1
kind: HelmChart
metadata:
  name: basic-kiosk-test 
  namespace: kiosk
spec:
  chart: oci://???
  valuesContent: |-
    X11:
      image:
        repository: ???
        pullPolicy: IfNotPresent
        # Overrides the image tag whose default is the chart appVersion.
        tag: latest
      keyboardModMap: |
        ; turn off 'a'
        keycode 38 = 
        

    pulseaudio:
      image:
        repository: ???
        pullPolicy: IfNotPresent
        # Overrides the image tag whose default is the chart appVersion.
        tag: latest
    workload:
      image:
        repository: ???
        pullPolicy: IfNotPresent
        tag: latest
      
      url: "http://172.16.0.1:4000/inputTest"
